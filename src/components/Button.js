



const Button = ({ name }) => {
  return (
    <button className="px-3 py-2 mx-1 rounded-lg bg-gray-100 text-[14px] font-medium whitespace-nowrap">
      {name}
    </button>
  );
};

export default Button;